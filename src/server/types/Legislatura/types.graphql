type Legislatura {
  id: ID!
  dataFim: String
  dataInicio: String
  uri: String
}

type LegislaturaCursor {
  pageInfo: PageInfo!
  edges: [LegislaturaEdge]
}

type LegislaturaEdge {
  node: Legislatura
  cursor: String
}
